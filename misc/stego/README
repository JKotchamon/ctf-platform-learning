**Stego (PNG LSB)**.

## Quick Start

**Requirements:** Docker, Docker Compose, Python 3.9+, Pillow.

```bash
# 1) Install Python deps
pip install -r requirements.txt

# 2) Build artifact + image (generate cover.png with your flag)
./build.sh medium "CTF{your_flag_here}"

# 3) Run (Compose)
docker compose up -d

#    Or run with plain Docker
docker run --rm -p 8081:80 ctf-stego-medium

# 4) Open the site
# http://localhost:8081
