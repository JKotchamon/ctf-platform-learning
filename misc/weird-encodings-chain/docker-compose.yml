version: "3.9"
services:
  weird-encodings:
    build: .
    environment:
      # optional: set your own flag; otherwise a random one is generated
      FLAG: "CTF{onion_layers_are_fun}"
    ports:
      - "8081:8081"
    restart: unless-stopped
